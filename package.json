{
  "name": "elc-partnership-builder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "agents": "^0.17.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "^3.2.0",
    "wrangler": "^4.105.0"
  }
}
