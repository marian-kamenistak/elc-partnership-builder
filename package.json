{
  "name": "elc-partnership-builder",
  "version": "1.0.0",
  "description": "MCP server: build and price a partnership with Engineering Leaders Community (3,100+ engineering leaders, Central Europe) from your AI assistant. Real catalog prices, a 12-month plan, 16% AI-channel discount.",
  "private": true,
  "scripts": {
    "deploy": "bash '/Users/mariankamenistak/ai/_platform/secret-sync/sync-secrets.sh' && wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "agents": "^0.17.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "^3.2.0",
    "wrangler": "^4.105.0"
  }
}
